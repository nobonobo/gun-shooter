# gun-shooter

# scenes
- top
  - create room -> room with hostID
- room with hostID
  - start game -> game
  - join room -> scope (Connect(dstID))
- game
  - quit -> room with hostID
- scope
  - quit -> room with hostID
